spring:
  application:
    name: security-service
eureka:
  instance:
    appname: security-service
  client:
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:9001/eureka
service:
  auth:
    url: http://localhost:8091/auth/realms/ecommerce/protocol/openid-connect/token
logging:
  file: ELK/logs/elk-stack.log
